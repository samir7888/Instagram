import React from 'react'

const Input = ({email}) => {
  return (
    <div>
        <input type={email} placeholder={} />
        <input type={} placeholder={} />
        <button>

        </button>
    </div>
  )
}

export default Input